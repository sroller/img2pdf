# frozen_string_literal: true

RSpec.describe Img2pdf do
  it "has a version number" do
    expect(Img2pdf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
